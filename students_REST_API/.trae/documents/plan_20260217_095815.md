I will create a comprehensive Spring Boot application to manage student records with PostgreSQL integration, including all requested REST APIs and SQL scripts.

### 1. Project Initialization & Configuration
- Create the project structure (Maven).
- **`pom.xml`**: Add dependencies for Spring Web, Data JPA, PostgreSQL Driver, and Lombok.
- **`src/main/resources/application.properties`**: Configure the PostgreSQL database connection (URL, username, password) and Hibernate settings.

### 2. Database Script
- **`src/main/resources/sql/init.sql`**: Create a SQL file containing:
    - Table creation query for `student` table with constraints.
    - Dummy data insertion queries for testing all scenarios (various schools, standards, genders, pass/fail percentages).

### 3. Backend Implementation
- **Entity (`Student.java`)**: Define the `Student` class with fields: `regNo` (Primary Key), `rollNo`, `name`, `standard`, `school`, `gender`, `percentage`.
- **Repository (`StudentRepository.java`)**: Extend `JpaRepository` and define custom query methods for:
    - Finding by School.
    - Counting by School.
    - Counting by Standard.
    - Counting by Gender and Standard.
    - Finding by Percentage (for result filtering).
- **Service (`StudentService.java`)**: Implement business logic:
    - Handle standard CRUD operations.
    - Implement logic for "pass/fail" results (>= 40%) and sorting.
    - Handle partial updates (PATCH).
- **Controller (`StudentController.java`)**: Implement the REST endpoints as requested:
    - `GET /students`: Get all.
    - `GET /students/{regNo}`: Get by Registration Number.
    - `POST /students`: Create new student.
    - `PUT /students/{regNo}`: Update student.
    - `PATCH /students/{regNo}`: Partial update.
    - `DELETE /students/{regNo}`: Delete student.
    - `GET /students/school`: Get students by school name.
    - `GET /students/school/count`: Count students by school name.
    - `GET /students/school/standard/count`: Count students by standard.
    - `GET /students/result`: Get students based on pass/fail status (sorted by percentage).
    - `GET /students/strength`: Count students by gender and standard.

### 4. Verification
- I will verify the implementation by ensuring all files are created correctly.
- I will provide the SQL script separately as requested so you can run it in your database tool.
