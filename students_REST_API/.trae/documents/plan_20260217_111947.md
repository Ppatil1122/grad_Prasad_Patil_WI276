The investigation confirms that the **application server is still running the old version of the code**, which is why the changes (renaming `name` to `school`) haven't taken effect.

**Evidence:**
1.  The **OLD** endpoint `GET /students/school?name=DPS` **still works** (returned status 200 and data).
2.  The **NEW** endpoint `GET /students/school?school=DPS` fails with `400 Bad Request`.
3.  The **NEW** endpoint `GET /students/standard/count` fails with `404 Not Found`.
4.  The **OLD** endpoint `GET /students/school/standard/count` **still works**.

This proves that although the source code files (`StudentController.java`) are updated, the running application process (PID 24744) has not been updated.

**Action Plan:**
1.  **Stop** the currently running application.
2.  **Restart** the application. This will trigger a recompilation of the Java code, applying the fixes I made (renaming `name` -> `school` and fixing the URL paths).
3.  Once restarted, the APIs will work as expected.

I will provide the correct API usage commands for you to test after the restart.
